#[cfg(test)]
mod unit_tests {
    use stm_add::*;

    #[test]
    fn trivially_true_test() {
        // A test template to be deleted eventually
        let var = "/usr/share/";
         
        assert_eq!(var, "/usr/share/");
    }
}
